<h3 style="color: #FF4433;">User Post</h3>
<div class="container">
  <div class="post"> 
     <div class="post-header">
       <h3>Post &#35;<%=@post.id %> by <%= @user.name %><%= link_to '🖋', edit_user_post_path(@user,@post) %> <%= button_to '🗑', user_post_path(@user,@post), method: :delete,data: { confirm: 'Are you sure?' } %></h3>
       <p class = "profile-stats"><span class= "comments-stats">Comments: <%= @post.comments_counter %></span><span class= "likes-stats"><%= button_to '🖒Likes',user_post_likes_path(@user,@post), method: :post %>:<%= @post.likes_counter %> </span></p>
     </div>
     <p> <%= @post.text %> </p>
  </div>
  <div class= "posts-comment"> 
     <% @post_comments.each do |comment| %>
       <% if comment.post_id == @post.id && comment.user.id == comment.user_id %>
        <p style= "color: #900C3F; padding: 5px;">
         <%= comment.user.name %> : 
         <%= comment.text %> 
        </p>
     <% end %>
    <% end %>
  </div>
  <h3 style="color: #FF4433;"><%= link_to 'Add Comment', new_user_post_comment_path(@user,@post) %></h3>
  <h3 style="color: #FF4433;"><%= link_to 'Back posts 🔙', user_posts_path %></h3>
</div>
